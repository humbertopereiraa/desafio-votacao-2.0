/* tslint:disable:no-unused-variable */

import { FormartarDataPipe } from './formartarData.pipe';

describe('Pipe: FormartarDatae', () => {
  it('create an instance', () => {
    let pipe = new FormartarDataPipe();
    expect(pipe).toBeTruthy();
  });
});
