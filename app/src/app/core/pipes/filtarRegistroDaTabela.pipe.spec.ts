/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FiltarRegistroDaTabelaPipe } from './filtarRegistroDaTabela.pipe';

describe('Pipe: FiltarRegistroDaTabelae', () => {
  it('create an instance', () => {
    let pipe = new FiltarRegistroDaTabelaPipe();
    expect(pipe).toBeTruthy();
  });
});
